# Time window in seconds for detecting port scans
TIME_WINDOW = 10

# Threshold for the number of suspicious ports accessed to trigger an alert
PORT_SCAN_THRESHOLD = 2

# Set of safe ports that are commonly used and can be ignored
SAFE_PORTS = {80, 443}

# Set of unsafe ports 
UNSAFE_PORTS = {}
